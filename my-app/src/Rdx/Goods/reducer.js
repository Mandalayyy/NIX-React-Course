import { GET_GOODS_LIST} from "./action";

const initialState = {
    data: [],
}

export const reducer = (state = initialState, action) =>{
    switch(action.type) {
      
        default:
            return state;
    }
}